# Rebrand: Light Finance → Nivesh.money — Complete Rename

> **Instructions:** Work through every section in order. Replace ALL references to the old brand. Run `pnpm build` after all changes to verify. Do NOT ask for human input until done.

**Old brand:** Light Finance / lightfinance.in
**New brand:** Nivesh.money / nivesh.money
**Display name:** Nivesh.money (with the dot — it IS the domain)
**Short form:** Nivesh (when the .money suffix is implied by context)
**Domain:** https://nivesh.money
**Tagline:** "Smart Mutual Fund Advisory" (replace "Expert Mutual Fund Advisory")

---

## 1. ENVIRONMENT FILES

### .env.local
```
NEXT_PUBLIC_SITE_URL=https://nivesh.money
CONTACT_EMAIL=hello@nivesh.money
```

### .env.example
```
NEXT_PUBLIC_SITE_URL=https://nivesh.money
CONTACT_EMAIL=hello@nivesh.money
```

---

## 2. package.json

Change:
```json
"name": "dad-finance"
```
To:
```json
"name": "nivesh-money"
```

---

## 3. src/app/layout.tsx (HIGHEST PRIORITY — 10+ references)

Replace ALL occurrences:

| Old | New |
|-----|-----|
| `Light Finance` | `Nivesh.money` |
| `lightfinance.in` | `nivesh.money` |
| `https://lightfinance.in` | `https://nivesh.money` |
| `Light Finance \| Expert Mutual Fund Advisory` | `Nivesh.money \| Smart Mutual Fund Advisory` |
| `Light Finance \| Mutual Fund Advisory` | `Nivesh.money \| Smart Mutual Fund Advisory` |
| `%s \| Light Finance` | `%s \| Nivesh.money` |
| `alt: 'Light Finance - Mutual Fund Advisory'` | `alt: 'Nivesh.money - Smart Mutual Fund Advisory'` |

In the metadata description, update:
```
Old: "AMFI-registered mutual fund distributor offering SIP, SWP, retirement planning, and goal-based investing. Personalized financial advisory for your future."
New: "AMFI-registered mutual fund distributor offering SIP, SWP, retirement planning, and goal-based investing. Personalized financial advisory by Nivesh.money."
```

In the JSON-LD structured data:
```json
"name": "Nivesh.money",
"url": "https://nivesh.money",
```

In metadataBase:
```typescript
metadataBase: new URL('https://nivesh.money'),
```

---

## 4. src/app/page.tsx

Replace:
```
"Light Finance | Expert Mutual Fund Advisory"
→ "Nivesh.money | Smart Mutual Fund Advisory"
```

Update meta description similarly.

---

## 5. src/app/invest/layout.tsx

Replace any "Light Finance" in the title/metadata with "Nivesh.money".

---

## 6. src/app/about/page.tsx

Replace "Light Finance" in title/metadata.

---

## 7. src/components/about-content.tsx

Replace:
```
"About Light Finance" → "About Nivesh.money"
"Light Finance was founded with a simple belief" → "Nivesh.money was founded with a simple belief"
```

Keep the rest of the about story the same — just swap the brand name.

---

## 8. src/components/layout/navbar.tsx

Replace the logo/brand text:
```
"Light Finance" → "Nivesh.money"
```

If the logo is text-based, update it. If it links to `/`, keep that.

---

## 9. src/components/layout/footer.tsx

Replace:
```
"© 2026 Light Finance. All rights reserved."
→ "© 2026 Nivesh.money. All rights reserved."
```

Update ARN line (keep placeholder for now):
```
"AMFI Registered Mutual Fund Distributor | ARN: XXXXX"
```
(Leave ARN: XXXXX — the user will fill in the real number later)

---

## 10. src/components/shared/print-header.tsx

Replace:
```
"Light Finance" → "Nivesh.money"
```

---

## 11. src/components/contact-content.tsx

Replace email domain:
```
"hello@lightfinance.in" → "hello@nivesh.money"
```

If there's a website URL displayed, update to `nivesh.money`.

---

## 12. src/components/sections/hero.tsx

The hero text should stay mostly the same (it's about mutual fund advisory, not the brand name). But if "Light Finance" appears anywhere, replace with "Nivesh.money".

The AMFI badge text stays: "AMFI Registered Mutual Fund Distributor" — this is a credential, not a brand name.

---

## 13. LEGAL PAGES

### src/app/terms/page.tsx
Replace ALL "Light Finance" with "Nivesh.money" (4 occurrences).

### src/app/disclaimer/page.tsx
Replace "Light Finance" with "Nivesh.money".

### src/app/privacy-policy/page.tsx
Replace "Light Finance" with "Nivesh.money" in title and any body text.

---

## 14. src/app/blog/[slug]/page.tsx

Replace:
```
"Light Finance" in any blog metadata/footer → "Nivesh.money"
```

---

## 15. SITEMAP & ROBOTS

### next-sitemap.config.js
```javascript
siteUrl: process.env.NEXT_PUBLIC_SITE_URL || 'https://nivesh.money',
```

### public/robots.txt
```
Host: https://nivesh.money
Sitemap: https://nivesh.money/sitemap.xml
```

### public/sitemap.xml and public/sitemap-0.xml
These will be regenerated by `pnpm build` — just delete the existing files and let the build recreate them with the new URL.

```bash
rm public/sitemap.xml public/sitemap-0.xml
```

---

## 16. OG IMAGE

The files `public/og-image.png` and `public/og-image.svg` likely contain "Light Finance" branding.

**Update the SVG** (`public/og-image.svg`):
- Replace any "Light Finance" text with "Nivesh.money"
- Keep the same layout/colors
- Then export/convert to PNG to update `public/og-image.png`

If the SVG is complex, at minimum update the text content. The OG image should show:
- "Nivesh.money" as the main text
- "Smart Mutual Fund Advisory" as subtitle
- Clean, professional look matching the site's navy/emerald palette

---

## 17. FAVICON

Check if `src/app/favicon.ico` or any other icon files contain "LF" or "Light Finance" branding. If so, update to "N" or "NM" for Nivesh.money.

---

## 18. VERIFICATION

After all changes:

```bash
cd /Users/manavmht/dad-finance
pnpm build
```

Must pass with 0 errors.

Then verify:
- [ ] `grep -r "Light Finance" src/` returns ZERO results
- [ ] `grep -r "lightfinance" src/` returns ZERO results
- [ ] `grep -r "lightfinance" .env*` returns ZERO results
- [ ] `grep -r "lightfinance" public/` returns ZERO results
- [ ] `grep -r "lightfinance" next-sitemap*` returns ZERO results
- [ ] Navbar shows "Nivesh.money"
- [ ] Footer shows "© 2026 Nivesh.money"
- [ ] Page title shows "Nivesh.money | Smart Mutual Fund Advisory"
- [ ] About page says "About Nivesh.money"
- [ ] Contact page shows hello@nivesh.money
- [ ] Terms/Privacy/Disclaimer all say "Nivesh.money"
- [ ] Print header says "Nivesh.money"
- [ ] OG image updated
- [ ] robots.txt points to nivesh.money
- [ ] Build generates sitemap with nivesh.money URLs

**Only present to human when ALL checks pass.**
